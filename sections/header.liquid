{% comment %}
  Section: Header - 3-Tier Layout (Desktop) + Full Mobile System
  Desktop:
    - Announcement bar: sits above (separate section)
    - Middle tier: Logo strip on Soft Blush bg
    - Bottom tier: Desktop nav on Deep Garnet bg
  Mobile:
    - Top bar: hamburger | logo | cart + search
    - Sidebar drawer: tabbed sections with full nav
    - Bottom dock: Home | Shop | Wishlist | Cart | Account
  All CSS lives in assets/base.css
{% endcomment %}

<!--
  =============================================
  MOBILE: Sidebar Drawer - Full Screen, Accordion
  =============================================
-->
<div class="mnav" id="MobileNav" aria-hidden="true" role="dialog" aria-modal="true" aria-label="Navigation menu">
  <!-- Header: Garnet/Espresso strip with logo + CLOSE X button -->
  <div class="mnav__head">
    <a
      href="{{ routes.root_url }}"
      class="mnav__logo"
      onclick="document.getElementById('MobileNav').classList.remove('is-open')"
    >
      <img src="{{ 'MicandMac_Monogram.webp' | asset_url }}" alt="Mic &amp; Mac" width="24" height="24">
      <span>MIC &amp; MAC</span>
    </a>
    <button class="mnav__close" id="MNavClose" aria-label="Close menu">
      <span>CLOSE</span>
    </button>
  </div>

  <!-- Scrollable body: main navigation + contact details + brand quote -->
  <div class="mnav__body">
    <!-- ── 4 Main Menu Items ── -->
    <nav class="mnav__menu" aria-label="Mobile main navigation">
      <ul class="mnav__menu-list">
        <li class="mnav__menu-item">
          <a href="{{ routes.all_products_collection_url }}" class="mnav__menu-link">All Products</a>
        </li>
        <li class="mnav__menu-item">
          <a href="{{ routes.collections_url }}/new-arrivals" class="mnav__menu-link">New Arrivals</a>
        </li>
        <li class="mnav__menu-item">
          <a href="{{ routes.collections_url }}/best-sellers" class="mnav__menu-link">Bestsellers</a>
        </li>
        <li class="mnav__menu-item">
          <a href="/pages/our-story" class="mnav__menu-link">Our Story</a>
        </li>
      </ul>
    </nav>

    <!-- ── Contact Details Section ── -->
    <div class="mnav__contact" id="MNavContact">
      <p class="mnav__contact-label">Need help?</p>
      <div class="mnav__contact-details">
        <a href="mailto:care@micandmacstores.com" class="mnav__contact-link">care@micandmacstores.com</a>
        <a href="tel:+917339663196" class="mnav__contact-link">+91 73396 63196</a>
      </div>
      
      <div class="mnav__contact-cta">
        <a href="/pages/contact-us" class="mnav__contact-btn">
          <span>CONTACT US</span>
          <span class="mnav__btn-arrow">&rarr;</span>
        </a>
      </div>

      <div class="mnav__contact-socials">
        <a
          href="https://instagram.com/micandmacstores"
          class="mnav__social-icon-link"
          target="_blank"
          rel="noopener"
          aria-label="Instagram"
        >
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="2" width="20" height="20" rx="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
          </svg>
        </a>
        <a
          href="https://wa.me/917339663196"
          class="mnav__social-icon-link"
          target="_blank"
          rel="noopener"
          aria-label="WhatsApp"
        >
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
          </svg>
        </a>
      </div>
    </div>

    <!-- ── Brand quote box ── -->
    <div class="mnav__brand-quote">
      <p>
        "Artisan-formulated. Lab-certified.<br>
        Made in Coimbatore."
      </p>
    </div>

    <!-- Spacer -->
    <div style="height: 24px;"></div>
  </div>
  <!-- /mnav__body -->

  <!-- Footer: Account actions pinned at bottom -->
  <div class="mnav__footer">
    <a href="{{ routes.account_login_url }}" class="mnav__footer-btn mnav__footer-btn--primary">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
      </svg>
      Sign In
    </a>
    <a href="{{ routes.account_register_url }}" class="mnav__footer-btn"> Create Account </a>
  </div>
</div>
<!-- /mnav -->

<!-- Overlay -->
<div class="overlay" id="SiteOverlay"></div>

<!--
  =============================================
  MOBILE: Top Bar (mobile only, ≤1023px)
  =============================================
-->
<div class="mobile-topbar" id="MobileTopBar">
  <!-- Left: Hamburger -->
  <button class="mtb__burger" id="NavToggle" aria-label="Open navigation" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>

  <!-- Center: Logo -->
  <a href="{{ routes.root_url }}" class="mtb__logo">
    <img src="{{ 'MicandMac_Monogram.webp' | asset_url }}" alt="Mic &amp; Mac" width="28" height="28" loading="eager">
    <span>MIC &amp; MAC</span>
  </a>

  <!-- Right: Search + Cart -->
  <div class="mtb__actions">
    <button class="mtb__btn" id="SearchToggleMobile" aria-label="Search">
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
      >
        <circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/>
      </svg>
    </button>
    <a href="/cart" class="mtb__btn mtb__cart" aria-label="Cart">
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
      >
        <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/>
      </svg>
      {%- if cart.item_count > 0 -%}
        <span class="mtb__cart-badge" id="MtbCartCount">{{ cart.item_count }}</span>
      {%- else -%}
        <span class="mtb__cart-badge mtb__cart-badge--hidden" id="MtbCartCount"></span>
      {%- endif -%}
    </a>
  </div>
</div>

<!--
  =============================================
  DESKTOP: Full Header (≥1024px)
  =============================================
-->
<header class="site-header" id="SiteHeader">
  <!-- Tier 2: Logo strip (Soft Blush bg) -->
  <div class="site-header__middle">
    <div class="container container--fluid">
      <div class="site-header__inner">
        <!-- Left: Tagline (desktop only) -->
        <div class="site-header__tagline">
          <span class="tagline-text">LOVE YOUR SKIN. EVERY DAY. 365.</span>
        </div>

        <!-- Center: Logo -->
        <div class="site-header__logo">
          <a href="{{ routes.root_url }}" class="site-header__logo-link">
            {%- if section.settings.logo != blank -%}
              <img
                src="{{ section.settings.logo | image_url: width: 280 }}"
                alt="{{ shop.name }}"
                width="{{ section.settings.logo_width | default: 160 }}"
                height="auto"
                loading="eager"
                class="site-logo-img"
              >
            {%- else -%}
              <div style="display: flex; align-items: center; gap: 10px;">
                <img
                  src="{{ 'MicandMac_Monogram.webp' | asset_url }}"
                  alt="Mic &amp; Mac Monogram"
                  width="36"
                  height="36"
                  style="object-fit: contain;"
                >
                <span class="site-header__logo-text" style="margin-top: 4px;">MIC &amp; MAC</span>
              </div>
            {%- endif -%}
          </a>
        </div>

        <!-- Right: Action icons (desktop) -->
        <div class="site-header__actions">
          <!-- Search -->
          <button class="site-header__action-btn" id="SearchToggle" aria-label="Search">
            <svg
              width="21"
              height="21"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.3"
              stroke-linecap="round"
            >
              <circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/>
            </svg>
          </button>

          <!-- Wishlist (desktop only) -->
          <a
            href="/pages/wishlist"
            class="site-header__action-btn wishlist-btn"
            aria-label="Saved items"
            title="Saved items"
          >
            <svg
              width="21"
              height="21"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.3"
              stroke-linecap="round"
            >
              <path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
            </svg>
            <span class="cart-count wishlist-count is-hidden"></span>
            <span class="cart-count wishlist-count is-hidden"></span>
          </a>

          <!-- Account (Desktop) - Conditional state -->
          {%- if customer -%}
            <!-- Logged-in: link directly to account page -->
            <a href="/pages/my-account" class="site-header__action-btn" aria-label="Account">
              <svg
                width="21"
                height="21"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.3"
                stroke-linecap="round"
              >
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                <circle cx="12" cy="7" r="4"/>
              </svg>
            </a>
          {%- else -%}
            <!-- Guest: links to account page -->
            <a href="{{ routes.account_login_url }}" class="site-header__action-btn" aria-label="Sign in">
              <svg
                width="21"
                height="21"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.3"
                stroke-linecap="round"
              >
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                <circle cx="12" cy="7" r="4"/>
              </svg>
            </a>
          {%- endif -%}

          <!-- Cart -->
          <a href="/cart" class="site-header__action-btn site-header__cart" aria-label="Cart">
            <svg
              width="21"
              height="21"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.3"
              stroke-linecap="round"
            >
              <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/>
              <line x1="3" y1="6" x2="21" y2="6"/>
              <path d="M16 10a4 4 0 0 1-8 0"/>
            </svg>
            {%- if cart.item_count > 0 -%}
              <span class="cart-count" id="CartCount">{{ cart.item_count }}</span>
            {%- else -%}
              <span class="cart-count cart-count--hidden" id="CartCount"></span>
            {%- endif -%}
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Tier 3: Navigation strip - desktop only -->
  <div class="site-header__bottom">
    <div class="container">
      <nav class="site-nav" aria-label="Main navigation">
        <ul class="site-nav__list">
          <li class="site-nav__item">
            <a
              href="{{ routes.all_products_collection_url }}"
              class="site-nav__link site-nav__link--white{% if request.path == '/collections/all' %} is-active{% endif %}"
              >All Products</a
            >
          </li>

          <li class="site-nav__separator" aria-hidden="true"><span></span></li>

          <li class="site-nav__item">
            <a
              href="{{ routes.collections_url }}/new-arrivals"
              class="site-nav__link{% if request.path contains 'new-arrivals' %} is-active{% endif %}"
              >New Arrivals</a
            >
          </li>

          <li class="site-nav__separator" aria-hidden="true"><span></span></li>

          <li class="site-nav__item">
            <a
              href="{{ routes.collections_url }}/bestsellers"
              class="site-nav__link{% if request.path contains 'bestsellers' %} is-active{% endif %}"
              >Bestsellers</a
            >
          </li>

          <li class="site-nav__separator" aria-hidden="true"><span></span></li>

          <li class="site-nav__item">
            <a href="/pages/about-us" class="site-nav__link{% if request.path contains 'about' %} is-active{% endif %}"
              >Our Story</a
            >
          </li>
        </ul>
      </nav>
    </div>
  </div>
</header>

<!--
  =============================================
  MOBILE: Bottom Dock
  =============================================
-->
<nav class="bottom-dock" id="BottomDock" aria-label="Mobile navigation">
  <a
    href="{{ routes.root_url }}"
    class="bdock__item{% if request.path == '/' %} is-active{% endif %}"
    aria-label="Home"
  >
    <svg
      width="22"
      height="22"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="1.5"
      stroke-linecap="round"
    >
      <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
    </svg>
    <span>Home</span>
  </a>
  <a
    href="{{ routes.all_products_collection_url }}"
    class="bdock__item{% if request.path contains 'collections' %} is-active{% endif %}"
    aria-label="Shop"
  >
    <svg
      width="22"
      height="22"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="1.5"
      stroke-linecap="round"
    >
      <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/>
    </svg>
    <span>Shop</span>
  </a>
  <a
    href="/pages/wishlist"
    class="bdock__item{% if request.path contains 'wishlist' %} is-active{% endif %}"
    aria-label="Saved Items"
  >
    <span class="bdock__cart-wrap">
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
      >
        <path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
      </svg>
      <span class="bdock__badge wishlist-count is-hidden"></span>
    </span>
    <span>Saved</span>
  </a>
  <a href="/cart" class="bdock__item" aria-label="Cart">
    <span class="bdock__cart-wrap">
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
      >
        <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/>
      </svg>
      {%- if cart.item_count > 0 -%}
        <span class="bdock__badge" id="DockCartCount">{{ cart.item_count }}</span>
      {%- else -%}
        <span class="bdock__badge bdock__badge--hidden" id="DockCartCount"></span>
      {%- endif -%}
    </span>
    <span>Cart</span>
  </a>
  {%- if customer -%}
    <a
      href="/pages/my-account"
      class="bdock__item{% if request.path contains 'my-account' %} is-active{% endif %}"
      aria-label="Account"
    >
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
      >
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
      </svg>
      <span>Account</span>
    </a>
  {%- endif -%}
</nav>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image"
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Logo width (px)",
      "min": 80,
      "max": 280,
      "step": 10,
      "unit": "px",
      "default": 160
    }
  ]
}
{% endschema %}
